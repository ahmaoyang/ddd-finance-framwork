package com.example.cp.oms.spec;

/**
 * 所有的业务模式，统一定义在此.
 */
public interface Patterns {

    /**
     * 业务模式标签在此统一定义.
     */
    interface Tags {
        String B2B = "B2B";
        String B2C = "B2C";
    }
}
